{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "prisma generate && node scripts/db-migrate.cjs && next build",
  "crons": [
    {
      "path": "/api/ingest",
      "schedule": "0 * * * *"
    }
  ]
}
